# Общее

- [X] Добавить TODO в проект
- [X] Надо заменить все print на logging
- [X] Добавить makefile или justfile для того чтобы сэкономить время на запуски команд
- [X] Работает только через GIT

# Нейросети

- [X] Разобраться что такое skills
- [X] Найти для себя https://www.skills.sh/?q=python подходящие скилы по проектирование и качеству кода, чтобы нейросеть могла в production
  -- git-commit
  -- python-code-style
  -- python-design-patterns
  -- python-testing-patterns

# Базы данных

- [X] Посмотреть DBBeaver, возможно будет удобней чем нативный psql
- [X] Надо придумать какую-то табличку, в которой может быть много записей (десятки, а лучше сотни тысяч)
- [X] Надо нормализовать базу данных (Parsed)
- [X] Включить echo в sqlalchemy, чтобы понимать какие запросы генерирует ORM
- [X] Посмотреть планы выполнения SQL запрос на чтение через explain
- [X] Если запросы быстрые (быстрее чем 0.5 секунды), написать скрипт (команду) для генерации данных, в идеале чтобы в таблице было несколько миллионов записей (faker, factory_boy)
- [X] **Индексы в базах данных: BTree, Hash, GIN — когда и что используется**
  -- BTree: когда и зачем (диапазонные запросы, сортировка, сравнения)
  -- Hash: когда и зачем (точное равенство)
  -- GIN: когда и зачем (массивы, полнотекстовый поиск, JSONB)
  -- Применить к своему проекту и сравнить результаты (explain)
- [X] Добавить индексы и сравнить результаты
- [X] Добавить health check в docker compose
- [X] Попробовать запустить на пустой базе данных `poetry run alembic upgrade head`, если всё работает, то убрать создание через скрипт
- [X] **Разобраться что такое нормализация БД и первые 3 нормальные формы, нормализовать свою базу данных**  (*[Нормальные формы базы данных. Три нормальных формы, нормализация и денормализация БД](https://youtu.be/0kq99Y8m0gw)*)
- [X] **Сделать так, чтобы автоинкремент ID в базе данных не использовался для идентификации записи со spimex**
  -- Записи идентифицируются по бизнес-ключам, а не по trade_id: бюллетени — (exchange_id, url), сделки — (exchange_id, exchange_trade_id)
  -- TradeRepository: upsert в add_url/add, file_path обновляется по (exchange_id, url)
  -- Частичный уникальный индекс uix_exchange_url добавлен в models — сгенерировать миграцию через alembic autogenerate и применить
- [X] Сделать так, чтобы в теории в базе данных можно было хранить записи из любых бирж не создавая отдельные таблицы под каждую биржу (нормализация)
- [ ] Нарисовать UML диаграммы архитектуры базы данных с видами связи между таблицами
- [X] Оптимизация запросов для новичков (*[PostgreSQL and Slow Queries: What to Do When Indexes Don&#39;t Help?](https://youtu.be/AZF9QZeJ-v8)*)

# Теория (каждый блок пробуем применить к коду)

- [X] Разобраться с тем что такое SRP в SOLID, что такое актор и чем отличается SRP от SoC
- [X] **Разобраться в наследование, композиии, общих принципах проектироваие** (*[OOP Design in Python](youtu.be/BoQnDymJIs4)*)
- [X] Разобраться что такое OCP в SOLID и с помощью какие принципов и паттернов его можно достить (*[Принцип открытости закрытости, С#, Unity](https://youtu.be/qOdf5CqEx-k?list=PL7GaNwuzA5StUNiOJFulHSD1SKkLdQYIx))*
- [X] Разобраться что такое DIP в SOLID (*[youtu.be/7fCJ6lCZNM](https://youtu.be/Wtl7dHqIRVc))* и что такое Dependecy Injection
- [X] **Разобраться с контекстными менеджерами: уметь писать через функции, `contextlib.contextmanager` и классы**
- [X] Разобраться что такое ValueObject и Entity, выделить Entity в проекте
- [X] **Уметь объяснять Dependency Inversion Principle (DIP)** (*[Принцип инверсии зависимости, С#, Unity](https://youtu.be/Uq10IqZhf7U))*
- [X] **Прочитать первые 2 главы книги** (*[книга google.disk](drive.google.com/file/d/15tPCAl7AA_arDNg7lh5t0d3ZnGOMxEpM/view))* **и уметь рассказывать**
- [X] Использовать ABC и `abc.abstractmethod` вместо Protocol
- [X] **Уметь объяснять Single Responsibility Principal (**SRP**)**
- [X] **Разобраться, зачем нужна нормализация БД и первые 3 нормальные формы, уметь приводить примеры нарушения**

# Рефакторинг структуры проекта

- [X] Переименовать `core/interfaces` -> `domain/interfaces/parsers`
- [X] Переименовать `domain/protocols` -> `domain/interfaces/repositories`
- [X] Переименовать папку `database` -> `infra/database`
- [X] Переименовать `datasource` -> `application/sources`
- [X] Переименовать `downloader` -> `application/downloaders`
- [X] Переименовать `extractor` -> `application/extractors`
- [X] Переименовать `parsers` -> `application/parsers`
- [X] DataSource переписать на композицию вместо наследования
- [X] Убрать orchestrator.py
- [X] Для каждого репозитория описать интерфейс через ABC (например `BaseUploadRepository`)
- [X] Проверить относительный импорт для переменных окружения `.env`, чтобы не было `BASE_DIR` и `ENV_PATH`
- [X] **Вынести exchange в отдельную таблицу** (нарушение нормальной формы на практике)
  -- Попробовать обновить название биржи в `trades` со `SPIMEX` на `SPIMEXX` через SQL
  -- Убедиться, что обновление затрагивает несколько таблиц/строк и это плохо (аномалии обновления)
  -- Вынести справочник бирж в отдельную таблицу `exchanges` и сослаться на неё через FK
  -- Обновить модели, сущности, репозитории и миграции (alembic)
- [X] **Убрать sources** — логика работы с биржей находится в downloader и parsers
  -- Удалить `src/application/sources/` и папку `src/domain/interfaces/parsers/datasource.py`
  -- Переписать `main.py` и `Orchestrator`, чтобы он работал без `DataSource`
- [X] **Подумать, может ли быть StopReason как Enum** (посмотреть что такое Enum)
  -- Изучить `enum.Enum` в Python
  -- Переписать `StopReason = str | None` на `Enum` (например `StopReason.CUTOFF`, `StopReason.MAX_DATE`, `StopReason.CONTINUE`)
  -- Заменить строковые литералы `"cutoff"`, `"max_date"` на значения Enum
- [X] **Разбить Parser на два класса**
  -- Один класс — отправляет запросы и получает ответы (fetch/html)
  -- Второй класс — разбирает HTML
  -- Обновить интерфейсы (`src/domain/interfaces/parsers/`) и DI в `main.py`

# Новый функционал

- [ ] Добавить click (Python **Click** - это популярный пакет для создания удобных интерфейсов командной строки (CLI) с помощью декораторов и минимального объема кода)
- [X] Добавить таблицу, в которой будут храниться уже разобранные документы или ссылки, чтобы их повторно не скачивать и не разбирать
- [X] Добавить возможность очистки таблицы выше, чтобы можно было запустить парсинг заново

# Брокеры и очереди

- [ ] Разобраться с терминологией (producer, consumer, брокер и т.д.)
- [ ] Посмотреть что такое модель Pub/Sub и подумать как её можно применить в проекте, например:
  -- producer отправляет в очередь A ссылки по которым нужно сходить и забрать xls/pdf
  -- consumer/producer читает из очереди A, отправляет запросы и складывает результат в другую очередь B
  -- consumer/producer читает из очереди B, парсинг и складывает в базу данных
- [ ] Реализовать на стандартных очередях, например в asyncio.Queue
- [X] **Система бронирования номера в отеле (средне)**
- [X] **Система бронирования номера в отеле V2 (сложно)**
- [X] **Софт для SDK банкомата (средне)**
